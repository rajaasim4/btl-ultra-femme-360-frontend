name = "your-worker-name"
main = "src/index.js" # or the path to your worker file
compatibility_date = "2025-10-19"

[site]
bucket = "./dist"      # path to your built site (Next.js, Vite, etc.)
entry-point = "workers" # if applicable

[build]
command = "npm run build"
